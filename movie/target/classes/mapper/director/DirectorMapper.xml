<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.movie.mapper.director.DirectorMapper">
    <select id="getDirectorByMovieId" parameterType="Integer" resultType="com.project.movie.domain.DO.Director">
        SELECT * FROM Director WHERE movieId = #{movieId}
    </select>
</mapper>